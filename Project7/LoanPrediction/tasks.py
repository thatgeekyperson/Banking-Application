import asyncio



def between_callback(duration, execute):
    # loop = asyncio.new_event_loop()
    # asyncio.set_event_loop(loop)
    # asyncio.run(threaded_task(duration, execute))
    # loop.run_until_complete(threaded_task(duration, execute))
    # loop.close()
    pass



